# Root node_modules (in case you use npm workspaces or yarn)
node_modules/

# Dependency directories in subfolders (like /agent and /server)
**/node_modules/

# Lock files in all subdirectories
**/package-lock.json
**/yarn.lock

# Optional: build artifacts or logs
dist/
*.log

# Environment files
.env
.env.local